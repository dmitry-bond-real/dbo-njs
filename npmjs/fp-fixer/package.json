{
  "name": "@dmitry_bond_real/fp-fixer",
  "version": "0.0.1",
  "description": "fixing floating-point values in objects to have appropriate scale",
  "keywords": [
    "floating-point",
    "scale",
    "ieee754"
  ],
  "license": "ISC", 
  "author": "dmitry_bond_real <dmitry.bond.real@gmail.com>", 
  "type": "commonjs", 
  "main": "./dist/fp-fixer.js", 
  "types": "./dist/fp-fixer.d.ts", 
  "scripts": {
    "start": "tsc && node ./index.js",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "^22.15.18",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "chalk": "^5.4.1"
  },
  "publishConfig": { "access": "private" }
}
